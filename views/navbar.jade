.container
  .navbar(style="margin-bottom:0px")
    .navbar-header
      a.navbar-brand(href='/')
        strong.theme-blue Cheuka's DOTA
        a.navbar-toggle(data-toggle='collapse', data-target='.navbar-collapse')
          span.icon-bar
          span.icon-bar
          span.icon-bar
    #navbar.navbar-collapse.collapse
      ul.nav.navbar-nav
        if user
          li(id='nav-request'): a(href="/request") REQUEST 
          //each val, key in constants.navbar_pages
           // if !val.hide || config.NODE_ENV === "development"
             // li(id='nav-#{key}'): a(href="/"+key, class=val["new-feature"] ? "new-feature" : "")=val.name
          li: a(href="/upload", class="new-feature") UPLOAD
          li: a(href="banpick", class="new-feature") Mock BP
        else
          li: a Log in to view options

      ul.nav.navbar-nav.navbar-right
        if user 
          if admin || user == 'admin'
            li: a(href="/admin") Admin
          else
            li: a(href="/center") Center!
          //li: a(href="/players/"+user.account_id) #{user.account_id}
          li: a(href="/logout") Logout
        else
          li: a(href="/") Login
          li: a(href="/register") Register


    //
      ul.nav.navbar-nav.navbar-right
        if user
          li: a(href="/players/"+user.account_id) Profile
          li: a(href="/logout") Logout
        else
          li: a(href="/login") Login
